---
ms.date: 07/28/2026
ms.topic: concept-article
description: Create an Excel custom function that recalculates whenever Excel recalculates, even when its arguments haven't changed.
title: Volatile values in functions
ms.localizationpriority: medium
ai-usage: ai-assisted
---

# Volatile values in functions

A volatile function recalculates whenever Excel recalculates, even if none of the function's arguments changed. Use a volatile custom function for values that can change independently of their inputs, such as dates, times, random numbers, and simulation results.

> [!NOTE]
> Several built-in Excel functions are volatile, such as `RAND`, `TODAY`, and `NOW`.

To mark an autogenerated custom function as volatile, add the `@volatile` tag to its JSDoc comment. For more information about autogenerated metadata, see [Autogenerate JSON metadata for custom functions](custom-functions-json-autogeneration.md).

## Create a volatile custom function

The following example uses `@volatile` to create a custom function that simulates rolling a six-sided die.

:::image type="content" source="../images/six-sided-die.gif" alt-text="GIF showing a custom function returning a random value to simulate rolling a six-sided die.":::

```js
/**
 * Simulates rolling a six-sided die.
 * @customfunction
 * @volatile
 */
function roll6sided() {
  return Math.floor(Math.random() * 6) + 1;
}
```

## Next steps

- [Custom functions parameter options](custom-functions-parameter-options.md)

## See also

- [Manually create JSON metadata for custom functions](custom-functions-json.md)
- [Create custom functions in Excel](custom-functions-overview.md)
